{
  "version": "0.0.1",
  "configurations": [
    {
      "name": "snowbrawl-dev",
      "runtimeExecutable": "sh",
      "runtimeArgs": [
        "-c",
        "set -a; [ -f .env ] && . ./.env; set +a; exec env -u HTTP_PROXY -u HTTPS_PROXY -u http_proxy -u https_proxy go run ./cmd/snowbrawl-server --web-dir web --admin-token dev"
      ],
      "port": 8080
    }
  ]
}
